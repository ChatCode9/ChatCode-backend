spring:
  config:
    activate:
      on-profile: log
  jpa:
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true
logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE
    com.chatcode: DEBUG
#    org.jooq.tools.LoggerListener: DEBUG

